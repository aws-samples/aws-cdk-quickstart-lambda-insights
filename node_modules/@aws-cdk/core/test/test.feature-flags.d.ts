import { Test } from 'nodeunit';
declare const _default: {
    isEnabled: {
        'returns true when the flag is enabled'(test: Test): void;
        'falls back to the default'(test: Test): void;
        'invalid flag'(test: Test): void;
    };
};
export = _default;
